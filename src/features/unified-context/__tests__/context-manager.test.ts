import { describe, it, expect } from 'vitest';

// TODO: Integration tests require MCP Memory server setup
describe.skip('UnifiedContextManager', () => {
  it('integration tests require MCP Memory server', () => {
    expect(true).toBe(true);
  });
});
